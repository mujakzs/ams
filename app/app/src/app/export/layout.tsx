export const metadata = {
  title: 'Export | AMS',
  description: 'Anthropometric Measurement System.',
};

export default function AnthropometicLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
